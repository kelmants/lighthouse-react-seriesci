version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10.16-browsers
    steps:
      - checkout
      - run: yarn install
      - run: yarn build
      - run: sudo yarn global add @lhci/cli@0.4.x
      - run: lhci autorun
